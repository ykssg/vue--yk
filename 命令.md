# 1. 改版本号（手动改 package.json 或用 npm version）
  npm version patch   # 0.0.1 → 0.0.2  修bug
  npm version minor   # 0.0.2 → 0.1.0  新功能
  npm version major   # 0.1.0 → 1.0.0  大版本

  # 2. 构建
  pnpm build

  # 3. 提交版本号 + tag
  git add package.json
  git commit -m "chore: v0.0.2"
  git tag v0.0.2

  # 4. 推送代码和 tag
  git push
  git push --tags

  # 5. 发布到 npm（可选）
  npm publish

  版本规则（语义化）

  ┌───────┬───────────────────┬───────────────┬────────────┐
  │ 类型  │       命令        │     示例      │    场景    │
  ├───────┼───────────────────┼───────────────┼────────────┤
  │ patch │ npm version patch │ 0.0.1 → 0.0.2 │ 修 bug     │
  ├───────┼───────────────────┼───────────────┼────────────┤
  │ minor │ npm version minor │ 0.0.1 → 0.1.0 │ 加新组件   │
  ├───────┼───────────────────┼───────────────┼────────────┤
  │ major │ npm version major │ 0.0.1 → 1.0.0 │ 不兼容改动 │
  └───────┴───────────────────┴───────────────┴────────────┘

  你现在版本是 0.0.1，开发阶段建议用 patch 或 minor 递增。